# Copy this file to .env.local and fill in values.
# .env.local is gitignored — never commit real credentials.

# Supabase (find in: supabase.com → project → settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # server-side only — never expose to browser

# QuickPay (find in: manage.quickpay.net → settings → integration)
QUICKPAY_API_KEY=
QUICKPAY_PRIVATE_KEY=             # used to validate webhook HMAC signatures

# Resend (find in: resend.com → API keys)
RESEND_API_KEY=
RESEND_FROM_EMAIL=                # e.g. noreply@havnebooking.dk

# App
NEXT_PUBLIC_APP_DOMAIN=           # e.g. havnebooking.dk (no protocol, no trailing slash)
NEXT_PUBLIC_SITE_URL=             # canonical URL for the tenant, e.g. https://hundested.havnebooking.dk

# E2e tests (Playwright) — global-setup creates the admin user automatically
E2E_ADMIN_EMAIL=e2e-admin@hundested-test.dk
E2E_ADMIN_PASSWORD=e2e-testpass-123
E2E_SUPER_EMAIL=super@havnebooking.dk
E2E_SUPER_PASSWORD=changeme123
